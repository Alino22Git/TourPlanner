<Window x:Class="TourPlanner.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TourPlanner"
        mc:Ignorable="d"
        Title="Tour Planner" Height="450" Width="800"
        DataContext="{Binding RelativeSource={RelativeSource self}}" >

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Menu  Grid.ColumnSpan="5">
            <MenuItem Header="File"/>
            <MenuItem Header="Edit"/>
            <MenuItem Header="Options"/>
            <MenuItem Header="Help"/>
        </Menu>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Grid.ColumnSpan="5" Margin="5">
            <TextBox Width="790" />
        </StackPanel>

        <Menu  Grid.Row="2"  Grid.ColumnSpan="2" Grid.RowSpan="3">
            <MenuItem Header="Tours">
                <DataGrid AutoGenerateColumns="True" ItemsSource="{Binding TourData}" />
            </MenuItem>
        </Menu>

        <Menu  Grid.Row="2" Grid.Column="2"  Grid.ColumnSpan="3" Grid.RowSpan="2">
            <MenuItem Header="General"/>
            <MenuItem Header="Route"/>
            <MenuItem Header="Misc"/>
        </Menu>

        <Menu  Grid.Row="4" Grid.Column="2"  Grid.ColumnSpan="3">
            <MenuItem Header="Tour Logs">
                <DataGrid AutoGenerateColumns="True" ItemsSource="{Binding TourLogData}" />
            </MenuItem>
        </Menu>
    </Grid>
</Window>